#!/bin/sh

DISTRO=`cat /etc/*-release | grep ^ID= | grep -Po '".*?"' | tr -d '"'`

cat <<EOF
This is a S2I nodejs-6 ${DISTRO} base image:
To use it, install S2I: https://github.com/kubesphere/s2ioperator

Sample invocation:

s2i build https://github.com/kubesphere/s2i-nodejs-container.git --context-dir=6/test/test-app/ ${NAMESPACE}/nodejs-69-${DISTRO}7 nodejs-sample-app

You can then run the resulting image via:
docker run -p 8080:8080 nodejs-sample-app
EOF
